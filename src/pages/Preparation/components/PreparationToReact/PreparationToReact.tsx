import React from 'react';
import PageLayout from '../../../../components/Layout/PageLayout';

const PreparationToReact: React.FC = () => {
  return (
    <PageLayout pageHeading="Preparation to React">
      <div className="test-components">Prepair to React</div>
    </PageLayout>
  );
};

export default PreparationToReact;
